name: deploy-non-prd
on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v1.6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECTKEY }}
        
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      
      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v1
      
      - name : Create env file
        run : |
          touch .env
          echo DATABASE_URL=${{ secrets.DATABASE_URL }} >> .env
          echo ORIGIN=${{ secrets.ORIGIN }} >> .env
          echo Token_key=${{ secrets.TOKEN_KEY }} >> .env

      - name: Build Docker image
        run : docker build . --file Dockerfile --tag ${{ secrets.DOCKER_HUB_USERNAME }}/gkey-back-end:latest

      - name : Push Docker image to registry
        run : |
          docker images
          docker push ${{ secrets.DOCKER_HUB_USERNAME }}/gkey-back-end:latest
          
      - name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}

  deploy:
    needs: build
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      # - name: Set env
      #   run: echo "ENV_NAME=$( [ "$GITHUB_REF" == "refs/heads/main" ] && echo 'uat' || echo ${GITHUB_REF##*/} )" >> $GITHUB_ENV
        
      - name: deploy with Execute SSH commmands on remote server
        uses: JimCronqvist/action-ssh@0.1.1
        with:
          hosts: ${{ secrets.APP_HOST }}
          privateKey: ${{ secrets.PRIVATE_KEY }}
          command: |
            docker login -u ${{ secrets.DOCKER_HUB_USERNAME }} -p ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
            docker rmi gkey-back-end:latest
            docker pull ${{ secrets.DOCKER_HUB_USERNAME }}/gkey-back-end:latest
            docker images
            docker-compose -f ~/compose/docker-compose.back.yml down
            docker-compose -f ~/compose/docker-compose.back.yml up -d
            docker ps -a
